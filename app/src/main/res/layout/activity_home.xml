<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <ImageView
        android:id="@+id/bgImage"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:scaleType="centerCrop" />

    <!-- Pages -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/pager"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingTop="36dp"
        android:paddingBottom="104dp"
        android:clipToPadding="false" />

    <!-- Page dots -->
    <LinearLayout
        android:id="@+id/dots"
        android:layout_width="wrap_content"
        android:layout_height="18dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="112dp"
        android:gravity="center"
        android:orientation="horizontal" />

    <ImageButton
        android:id="@+id/editMenuBtn"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|start"
        android:layout_margin="14dp"
        android:background="@android:color/transparent"
        android:src="@android:drawable/ic_menu_sort_by_size"
        android:tint="#FFFFFF"
        android:visibility="gone" />

    <LinearLayout
        android:id="@+id/editPanel"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:layout_gravity="top|start"
        android:layout_marginTop="64dp"
        android:layout_marginStart="14dp"
        android:orientation="vertical"
        android:padding="12dp"
        android:background="#AA111111"
        android:visibility="gone">

        <TextView android:id="@+id/btnBgFolded" android:text="Set folded background"
            android:textColor="#FFFFFF" android:padding="10dp"/>

        <TextView android:id="@+id/btnBgUnfolded" android:text="Set unfolded background"
            android:textColor="#FFFFFF" android:padding="10dp"/>

        <TextView android:id="@+id/btnTheme" android:text="Change theme"
            android:textColor="#FFFFFF" android:padding="10dp"/>

        <TextView android:id="@+id/btnWidgets" android:text="Add widgets"
            android:textColor="#FFFFFF" android:padding="10dp"/>

        <TextView android:id="@+id/btnApps" android:text="Add apps"
            android:textColor="#FFFFFF" android:padding="10dp"/>
    </LinearLayout>

    <!-- iOS-style dock -->
    <FrameLayout
        android:id="@+id/dockGlass"
        android:layout_width="match_parent"
        android:layout_height="92dp"
        android:layout_gravity="bottom"
        android:layout_margin="16dp"
        android:padding="10dp">

        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/dockRow"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:overScrollMode="never" />
    </FrameLayout>

</FrameLayout>
